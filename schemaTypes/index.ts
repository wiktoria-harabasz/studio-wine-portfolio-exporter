import { producerType } from "./producerType"
import { regionType } from "./regionType"
import { subregionType } from "./subregionType"
import { grapeType } from "./grapeType"

export const schemaTypes = [producerType, regionType, subregionType, grapeType]
